de:
  button_create_new_repository: Neues Repository erstellen

  label_repository_format: Repository

  field_scm: SCM

  text_cannot_be_changed_later: Kann später nicht mehr geändert werden.
  text_cannot_be_used_redmine_auth: Die Authentifizierung über Redmine kann mit diesem Repository Namen nicht verwendet werden.

  activerecord:
    errors:
      messages:
        should_be_of_format_local: sollte dem Format "%{format}" entsprechen
        already_exists: existiert bereits
        scm_not_supported: Dieses SCM wird nicht unterstützt.
        repository_exists_for_identifier: Für diesen Projektnamen existiert bereits ein Repository.
