en:
  button_create_new_repository: Create new repository

  activerecord:
    errors:
      messages:
        should_be_of_format_local: should be of format "file://{{svn_path}}/<repository>/"
        already_exists: already exists
